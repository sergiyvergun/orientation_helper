name: orientation_helper
description: A Flutter plugin to easily handle screen orientation for every route in your app.
version: 1.0.1
homepage: https://github.com/sergiyvergun/orientation_helper

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Linter
  pedantic: ^1.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


